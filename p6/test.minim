int a;
bool b;

void main() {
    int t1;
    bool t2;
    int t3;
    t3 = 3;
    t2 = false;
    t1 = 1;
    
    
    t2 = t1 < t3;
    disp << (1 < 2);
    disp << "\n";
    disp << (1 < 1);
    disp << "\n";
    disp << (1 < 0);
    disp << "\n";
     disp << (1 <= 2);
    disp << "\n";
    disp << (1 <= 1);
    disp << "\n";
    disp << (1 <= 0);
    disp << "\n";
     disp << (1 > 2);
    disp << "\n";
    disp << (1 > 1);
    disp << "\n";
    disp << (1 > 0);
    disp << "\n";
     disp << (1 >= 2);
    disp << "\n";
    disp << (1 >= 1);
    disp << "\n";
    disp << (1 >= 0);
    disp << "\n";
}
