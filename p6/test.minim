int a;
bool b;

void f1(){
    int a;
    disp << "Reach f1\n";
}

void f2(int a){
    int b;
    disp << "Reach f2\n";
}

int f3(int a, int b){
    int c;

    c = a + b;
    return c;
    c = 10;
}

int f4(){
    return f3(3, 4);
}

bool f5(bool a, bool b){
    return b;
}

int recursion(int n){
    if(n == 1){
        return 1;
    } else {
        return n + recursion(n-1);
    }
}

void main() {
    int k;
    bool m;
    bool n;
    bool l;

    k = 5;
    m = true;
    n = false;

    f1();
    f2(k);

    disp << k;
    disp << "\n";

    k = f3(2, 4);
    disp << k;
    disp << "\n";

    k = f4();
    disp << k;
    disp << "\n";

    l = f5(m, n);
    disp << l;
    disp << "\n";

    k = recursion(5);
    disp << k;
    disp << "\n";


    
}
