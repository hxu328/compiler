int a;
bool b;

void main() {
    int c;
    bool d;

    int e;
    int f;

    bool k;
    bool w;

    a = 1;
    b = true;
    c = 2;
    d = false;

    e = a;
    k = b;
    f = c;
    w = d;

    disp << a;
    disp << "\n";
    disp << b;
    disp << "\n";
    disp << c;
    disp << "\n";
    disp << d;
    disp << "\n";

    disp << e;
    disp << "\n";
    disp << k;
    disp << "\n";
    disp << f;
    disp << "\n";
    disp << w;
    disp << "\n";

    disp << "Please input a value(int): ";
    input >> a;
    disp << a;
    disp << "\n";
}
