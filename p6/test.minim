int a;
bool b;

void main() {
    int t1;
    bool t2;
    int t3;
    t3 = 3;
    t2 = false;
    t1 = 1;
    
    
    disp << (1 != 2);
    disp << "\n";
    disp << (1 != 1);
    disp << "\n";
    disp << (true != true);
    disp << "\n";
    disp << (true != false);
    disp << "\n";
    disp << ("hello" != "hello");
    disp << "\n";
    disp << ("hello" != "world");
    disp << "\n";

    
}
